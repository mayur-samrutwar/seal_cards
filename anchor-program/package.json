{
  "name": "anchor-program",
  "version": "0.1.0",
  "description": "Solana program with getter and setter",
  "main": "lib/index.js",
  "scripts": {
    "build": "anchor build",
    "test": "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts",
    "test:anchor": "anchor test --skip-local-validator",
    "test:local": "anchor test",
    "deploy": "anchor deploy",
    "deploy:devnet": "anchor deploy --provider.cluster devnet"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.32.1"
  },
  "devDependencies": {
    "@types/bn.js": "^5.1.0",
    "@types/chai": "^4.3.0",
    "@types/mocha": "^9.0.0",
    "chai": "^4.3.0",
    "mocha": "^10.0.0",
    "ts-mocha": "^10.0.0",
    "typescript": "^4.3.5"
  }
}
